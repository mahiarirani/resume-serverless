{
  "name": "resume-template",
  "version": "0.1.0",
  "description": "a resume / cv template",
  "author": "Mahyar Iranibazaz",
  "scripts": {
    "dev": "rm -rf dist && eleventy && mkdir dist/assets/css && postcss tailwind.css > dist/assets/css/style.css && eleventy --serve",
    "build": "rm -rf dist && eleventy && mkdir dist/assets/css && NODE_ENV=production postcss tailwind.css > dist/assets/css/style.css"
  },
  "devDependencies": {
    "@11ty/eleventy-navigation": "^0.3.5",
    "@11ty/eleventy-plugin-directory-output": "^1.0.1",
    "@tailwindcss/typography": "^0.5.10",
    "autoprefixer": "^10.4.16",
    "cssnano": "^6.0.1",
    "js-yaml": "^4.1.0",
    "luxon": "^3.4.3",
    "node-fetch": "^3.3.2",
    "postcss": "^8.4.31",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.3.3"
  }
}
